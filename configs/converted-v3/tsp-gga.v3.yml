schema: "3.0"

run:
  id: tsp-gga-v3
  name: "Converted from v2 sample: tsp-gga.yaml"
  masterSeed: 42
  deterministicStreams: true
  checkpointEveryIterations: 0

representation:
  type: permutation-vector
  size: 8

problem:
  type: small-tsp

algorithm:
  type: ehm-eda
  populationSize: 200
  elitism: 2
  selectionRatio: 0.5

model:
  type: ehm
  epsilon: 1.0e-6

selection:
  type: tournament
  k: 3

replacement:
  type: elitist

stopping:
  type: max-iterations
  maxIterations: 1000

constraints:
  type: identity

localSearch:
  type: none

restart:
  type: none

niching:
  type: none

observability:
  metricsEveryIterations: 10
  emitModelDiagnostics: true

persistence:
  enabled: true
  sinks: [console, csv, jsonl]
  outputDirectory: ./results
  database:
    enabled: false
    url: jdbc:sqlite:edaf-v3.db
    user: ""
    password: ""

reporting:
  enabled: true
  formats: [html]
  outputDirectory: ./reports

web:
  enabled: false
  port: 7070
  pollSeconds: 3

logging:
  modes: [console, jsonl, file]
  verbosity: normal
  jsonlFile: ./results/tsp-gga-v3-events.jsonl
  logFile: ./edaf-v3.log
