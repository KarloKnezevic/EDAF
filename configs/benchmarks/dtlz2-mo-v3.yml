schema: "3.0"

run:
  id: benchmark-dtlz2-mo-v3
  name: DTLZ2 on MO-EDA Skeleton (v3)
  masterSeed: 20260225
  deterministicStreams: true
  checkpointEveryIterations: 0

representation:
  type: real-vector
  length: 12
  lower: 0.0
  upper: 1.0

problem:
  type: dtlz
  functionId: 2
  objectives: 3
  scalarWeights: [0.34, 0.33, 0.33]

algorithm:
  type: mo-eda-skeleton
  populationSize: 220
  elitism: 2
  selectionRatio: 0.4

model:
  type: gaussian-diag
  minSigma: 1.0e-9

selection:
  type: truncation

replacement:
  type: elitist

stopping:
  type: max-iterations
  maxIterations: 150

constraints:
  type: repair

localSearch:
  type: none

restart:
  type: none

niching:
  type: none

observability:
  metricsEveryIterations: 10
  emitModelDiagnostics: true

persistence:
  enabled: true
  sinks: [console, csv, jsonl, db]
  outputDirectory: ./results/benchmarks
  database:
    enabled: true
    url: jdbc:sqlite:edaf-v3.db
    user: ""
    password: ""

reporting:
  enabled: false
  formats: [html]
  outputDirectory: ./reports/benchmarks

web:
  enabled: false
  port: 7070
  pollSeconds: 3

logging:
  modes: [console, jsonl, file, db]
  verbosity: normal
  jsonlFile: ./results/benchmarks/benchmark-dtlz2-mo-v3-events.jsonl
  logFile: ./edaf-v3.log
